// Placeholder (you said: no external API)
// Later you can draw your own markers or user location via Geolocation API.
const mapBox = document.getElementById("map");
if (mapBox) {
  mapBox.textContent = "Your location and nearby mechanics will appear here.";
}
